Per-pair probe traces (`*.probe.csv`, 60-230 MB each) exceed GitHub file
limits and are archived separately — Zenodo DOI: TBD (set at paper
submission). `analyze_trans.py` requires them; the transition-record CSVs
and meta.json in this directory are complete without them.
